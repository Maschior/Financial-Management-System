spring:
  datasource:
    url: jdbc:mysql://cbzlmdaassup06:3306/fms
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  name: FMS
  version: 0.1